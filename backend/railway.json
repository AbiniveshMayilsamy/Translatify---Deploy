{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "cd backend && gunicorn -w 1 -k gthread --threads 4 --bind 0.0.0.0:$PORT app:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
